# FocusLamp 嵌入式基础功能开发说明

## 1. 项目目标

本项目是一个基于 ESP32-P4 / ESP32P4C5 的机械臂智能台灯嵌入式系统。

当前阶段的开发目标不是直接实现复杂的“专注模式 / 陪伴模式 / 自定义模式”，而是先保证底层基础功能完整：

1. 灯光可以控制；
2. 5 个舵机可以控制；
3. 4 个按键 / 触摸键可以识别；
4. 雷达数据可以解析并发布事件；
5. 1.45 寸 TFT 屏幕可以显示基础状态；
6. 功放 / 喇叭可以播放声音并调节音量；
7. ~~感光模块可以读取环境光~~（光感模块已移除）；
8. 所有模块状态可以统一保存；
9. 上层可以通过统一接口调用这些基础能力。

后续专注模式、陪伴模式、自定义模式，都是在这些基础能力之上组合出来的。

---

## 2. 开发前必须先做的事情

在写任何新代码之前，必须先检查当前源代码已经实现了什么。

不要直接重构整个项目，不要重复造轮子。

请先完成以下代码审查任务：

### 2.1 检查源代码结构

需要重点确认：

| 检查项              | 需要判断                                   |
| ---------------- | -------------------------------------- |
| 是否已有灯光驱动         | 是否已有 PWM / LED / WS2812 控制             |
| 是否已有 LCD 驱动      | 是否已有 GC9107 / SPI / 背光控制               |
| 是否已有雷达解析         | 是否已有雷达串口接收和消息解析                        |
| 是否已有按键处理         | 是否已有 GPIO / TTP 按键扫描和消抖                |
| 是否已有舵机控制         | 是否已有 PWM / LEDC / MCPWM 舵机控制           |
| 是否已有音频输出         | 是否已有 I2S / 功放 / 喇叭播放代码                 |
| 是否已有状态管理         | 是否有全局状态结构体                             |
| 是否已有事件系统         | 是否有事件发布 / 订阅机制                         |
| 是否已有 FreeRTOS 任务 | 是否已有各模块 task                           |
| 是否已有通信接口         | 是否已有 WebSocket / UART / MCP / API 控制入口 |

---

## 3. 源代码验证报告要求

在继续添加功能前，必须先输出一份验证报告。

报告格式如下：

### 3.1 已有功能表

| 模块   | 源代码位置                     | 已实现功能      | 是否可用       | 备注   |
| ---- | ------------------------- | ---------- | ---------- | ---- |
| 灯光   | 例如 `main/light.c`         | 开关 / 亮度调节  | 是 / 否 / 部分 | 说明问题 |
| 舵机   | 例如 `main/servo.c`         | 单舵机角度控制    | 是 / 否 / 部分 | 说明问题 |
| 雷达   | 例如 `main/radar.c`         | 串口解析       | 是 / 否 / 部分 | 说明问题 |
| LCD  | 例如 `main/lcd.c`           | 初始化 / 显示文字 | 是 / 否 / 部分 | 说明问题 |
| 按键   | 例如 `main/button.c`        | 短按 / 长按    | 是 / 否 / 部分 | 说明问题 |
| 音频   | 例如 `main/audio.c`         | 播放 / 音量    | 是 / 否 / 部分 | 说明问题 |
| 状态管理 | 例如 `main/device_status.c` | 保存设备状态     | 是 / 否 / 部分 | 说明问题 |

### 3.2 缺失功能表

| 模块  | 缺失功能            | 影响          | 优先级 |
| --- | --------------- | ----------- | --- |
| 灯光  | 例如缺少 0-5 档亮度    | 无法统一控制灯光    | P0  |
| 舵机  | 例如缺少 5 舵机统一控制   | 机械臂无法完整控制   | P0  |
| 雷达  | 例如缺少心率事件发布      | 雷达数据不能被上层使用 | P0  |
| LCD | 例如缺少 GC9107 初始化 | 屏幕无法显示状态    | P0  |
| 按键  | 例如缺少长按识别        | 交互不完整       | P0  |

### 3.3 不允许做的事情

在基础功能没有确认完整之前，不要做以下事情：

1. 不要直接写复杂的专注模式；
2. 不要直接写陪伴模式动作；
3. 不要直接写自定义规则系统；
4. 不要重写整个工程结构；
5. 不要删除已有可用代码；
6. 不要把所有功能写在一个大文件里；
7. 不要写没有硬件接口支撑的假逻辑；
8. 不要只写 UI 或上层模式，忽略底层驱动。

---

## 4. 当前硬件定义

### 4.1 主控

```text
主控：ESP32-P4 / ESP32P4C5
开发环境：ESP-IDF
系统：FreeRTOS
```


## 5. 基础模块开发顺序

优先级按照 P0、P1、P2 执行。

| 优先级 | 模块     | 目标                     |
| --- | ------ | ---------------------- |
| P0  | 灯光模块   | 能开关，能 0-5 档调光          |
| P0  | 舵机模块   | 5 个舵机能单独控制，能回原位，有限位    |
| P0  | 按键模块   | 4 个按键能识别短按、长按、组合按键     |
| P0  | 雷达模块   | 能解析人体、呼吸、心率、位置、距离、跟踪事件 |
| P0  | LCD 模块 | GC9107 屏幕能初始化并显示基础状态   |
| P0  | 音频模块   | 能播放提示音 / TTS，能调音量      |
| ~~P1~~ | ~~感光模块~~   | ~~能读取环境光并划分等级~~（已移除）            |
| P1  | 状态管理   | 能统一保存所有模块状态            |
| P1  | 通信接口   | 能接收上层控制命令并返回状态         |
| P2  | 平滑控制   | 灯光渐变、舵机缓动              |
| P2  | 异常保护   | 雷达异常、舵机越界、屏幕异常处理       |

---

## 6. 灯光模块要求

### 6.1 基础目标

灯光模块只需要先完成最基础能力：

1. 开灯；
2. 关灯；
3. 设置亮度；
4. 增加亮度；
5. 降低亮度；
6. 返回当前亮度状态。

先不要做复杂模式灯效。

### 6.2 亮度档位

亮度统一分为 0-5 档：

| 档位 | 含义 |
| -- | -- |
| 0  | 关闭 |
| 1  | 很暗 |
| 2  | 偏暗 |
| 3  | 正常 |
| 4  | 偏亮 |
| 5  | 最亮 |


### 6.4 验收标准

| 测试       | 结果       |
| -------- | -------- |
| 设置 0 档   | 灯关闭      |
| 设置 1-5 档 | 亮度逐渐增加   |
| 超过 5 档   | 保持 5 档   |
| 低于 0 档   | 保持 0 档   |
| 查询状态     | 返回当前真实亮度 |

---

## 7. 5 舵机机械臂模块要求

### 7.1 硬件定义

当前机械臂为 5 个舵机，结构类似 LeRobot 机械臂。


### 7.2 基础目标

舵机模块必须先完成：

1. 单个舵机角度控制；
2. 5 个舵机统一控制；
3. 记录当前角度；
4. 回到初始姿态；
5. 角度限位；
6. 停止当前动作；
7. 简单平滑运动。




### 7.5 验收标准

| 测试           | 结果            |
| ------------ | ------------- |
| 控制 Servo 1   | 只有 Servo 1 转动 |
| 控制 Servo 2-5 | 对应舵机正常转动      |
| 输入超限角度       | 自动限制到安全范围     |
| 执行 home      | 5 个舵机回到默认角度   |
| 查询角度         | 返回当前记录角度      |
| 执行 stop      | 当前动作停止        |

### 7.6 暂时不要做

以下内容先不要写：

1. 自动跟随用户；
2. 根据雷达位置自动转向；
3. 音乐卡点动作；
4. 情绪动作；
5. 复杂逆运动学。

这些后面再基于基础舵机接口实现。

---

## 8. 4 按键模块要求

### 8.1 硬件定义

当前按键数量为 4 个：

| 按键            | 引脚     |
| ------------- | ------ |
| KEY_A / TTP_A | GPIO9  |
| KEY_B / TTP_B | GPIO22 |
| KEY_C / TTP_C | GPIO10 |
| KEY_D / TTP_D | GPIO23 |

### 8.2 基础目标

按键模块先完成事件识别，不绑定复杂业务。

需要支持：

1. 短按；
2. 长按；
3. 消抖；
4. 组合按键；
5. 事件上报。




### 8.5 验收标准

| 测试          | 结果             |
| ----------- | -------------- |
| 按 KEY_A     | 返回 KEY_A_SHORT |
| 长按 KEY_A    | 返回 KEY_A_LONG  |
| 按 KEY_B/C/D | 返回对应按键事件       |
| 同时按 A+B     | 返回组合键事件        |
| 按键抖动        | 不重复误触发         |

---

## 9. 雷达模块要求

### 9.1 当前雷达消息处理逻辑

雷达当前需要处理以下消息：

| 消息类型            | 处理逻辑                                                     |
| --------------- | -------------------------------------------------------- |
| HUMAN_DETECT    | 发布 `RADAR_EVENT_PRESENCE`，同时更新 `motion_analyzer` 的人体存在状态 |
| PHASE_TEST      | 解析 3 个 float，其中 offset 4 为呼吸相位，offset 8 为心相相位            |
| HEART_RATE      | 发布 `RADAR_EVENT_HEART_RATE`                              |
| POSITION        | 解析多个目标，首个目标包含 x / y / dop_idx / cluster_id               |
| TARGET_RANGE    | 发布 `RADAR_EVENT_TARGET_RANGE`                            |
| TARGET_TRACK    | 发布 `RADAR_EVENT_TARGET_TRACK`                            |
| FIRMWARE_STATUS | 默认打印日志，暂不做特殊业务处理                                         |

### 9.2 PHASE_TEST 详细要求

`PHASE_TEST` 中需要解析 3 个 float：

| 偏移       | 含义   |
| -------- | ---- |
| offset 4 | 呼吸相位 |
| offset 8 | 心相相位 |

处理逻辑：

1. 呼吸相位经过低通滤波；
2. 低通滤波后发布 `RADAR_EVENT_BREATH`；
3. 心相相位送入 `hrv_analyzer`；
4. `hrv_analyzer` 计算 HRV 指标；
5. HRV 指标包括 SDNN、RMSSD、pNN50。





### 9.6 验收标准

| 测试                 | 结果                        |
| ------------------ | ------------------------- |
| 收到 HUMAN_DETECT    | 发布人体存在事件                  |
| 收到 PHASE_TEST      | 解析呼吸相位、心相相位               |
| 收到 HEART_RATE      | 更新心率状态                    |
| 收到 POSITION        | 更新 x/y/dop_idx/cluster_id |
| 收到 TARGET_RANGE    | 更新目标距离                    |
| 收到 TARGET_TRACK    | 更新目标跟踪状态                  |
| 收到 FIRMWARE_STATUS | 打印日志，不影响系统                |
| 收到错误包              | 丢弃，不崩溃                    |

---

## 10. LCD 屏幕模块要求

### 10.1 硬件定义

当前屏幕为：

```text
1.45 寸 TFT 液晶屏
驱动芯片：GC9107
```


### 10.2 基础目标

LCD 模块先完成：

1. GC9107 初始化；
2. SPI 通信；
3. 背光控制；
4. 清屏；
5. 显示文字；
6. 显示基础状态；
7. 刷新页面。

### 10.4 基础显示内容

| 显示项  | 示例            |
| ---- | ------------- |
| 当前模式 | Mode: Default |
| 灯光档位 | Light: 3/5    |
| 音量档位 | Volume: 4/5   |
| 雷达人体 | Human: Yes    |
| 心率   | HR: 78        |
| 距离   | Range: 0.8m   |
| 舵机状态 | Arm: Ready    |

### 10.5 验收标准

| 测试    | 结果            |
| ----- | ------------- |
| 上电初始化 | 屏幕点亮          |
| 调用清屏  | 屏幕清空          |
| 显示文字  | 指定位置出现文字      |
| 显示状态页 | 显示灯光、雷达、音量等状态 |
| 背光设置  | 背光可开关或分档      |

---

## 11. 音频 / 功放 / 喇叭模块要求

### 11.1 基础目标

音频模块先完成：

1. 初始化音频输出；
2. 播放提示音；
3. 播放 TTS 音频；
4. 停止播放；
5. 音量调节；
6. 静音。

### 11.2 音量档位

| 档位 | 含义 |
| -- | -- |
| 0  | 静音 |
| 1  | 很小 |
| 2  | 偏小 |
| 3  | 正常 |
| 4  | 偏大 |
| 5  | 最大 |


### 11.4 验收标准

| 测试       | 结果       |
| -------- | -------- |
| 播放提示音    | 喇叭有声音    |
| 设置音量 0   | 静音       |
| 设置音量 1-5 | 音量变化     |
| 停止播放     | 当前声音停止   |
| 查询状态     | 返回是否正在播放 |

---

## 12. ~~感光模块要求~~（已随光感模块移除）

> 环境光传感器（TEMT6000/GPIO21 ADC）已从硬件移除，相关驱动与逻辑
> （light_sensor_driver、sensor_service 光感采样、环境光自动调光、/api/ambient
> 端点）一并下线。亮度改为固定五档，不再依据环境光动态调节。

---

## 13. 设备状态管理要求

### 13.1 目标

必须建立统一的设备状态结构。

不要每个模块自己保存一套状态，导致状态不同步。




### 13.4 验收标准

| 测试       | 结果                |
| -------- | ----------------- |
| 修改灯光     | 状态结构同步更新          |
| 修改舵机     | 对应舵机角度更新          |
| 收到雷达事件   | 雷达状态更新            |
| 按键触发     | last_key_event 更新 |
| LCD 显示状态 | 与状态结构一致           |

---




## 18. 最终交付要求

完成代码后，需要输出以下内容：

### 18.1 功能完成表

| 模块   | 是否完成  | 测试结果     | 备注 |
| ---- | ----- | -------- | -- |
| 灯光   | 是 / 否 | 通过 / 未通过 | 说明 |
| 舵机   | 是 / 否 | 通过 / 未通过 | 说明 |
| 按键   | 是 / 否 | 通过 / 未通过 | 说明 |
| 雷达   | 是 / 否 | 通过 / 未通过 | 说明 |
| LCD  | 是 / 否 | 通过 / 未通过 | 说明 |
| 音频   | 是 / 否 | 通过 / 未通过 | 说明 |
| ~~感光~~   | ~~是 / 否~~ | ~~通过 / 未通过~~ | ~~已随光感模块移除~~ |
| 状态管理 | 是 / 否 | 通过 / 未通过 | 说明 |
| 通信接口 | 是 / 否 | 通过 / 未通过 | 说明 |

### 18.2 缺失功能清单

如果有功能暂时无法完成，必须说明：

| 缺失功能        | 原因               | 需要补充的信息      |
| ----------- | ---------------- | ------------ |
| 例如舵机控制      | 缺少 5 个舵机 GPIO 引脚 | 需要确认舵机接线     |
| 例如音频播放      | 不清楚功放型号          | 需要确认功放接口     |
| 例如 LCD 显示异常 | GC9107 初始化参数不确定  | 需要确认屏幕分辨率和方向 |


## 21. 最终原则

本阶段开发原则：

```text
先验证已有代码；
再补缺失基础功能；
先让硬件能单独工作；
再统一状态和接口；
最后才做模式组合。
```

不要追求一次性把所有产品功能写完。

先保证基础功能稳定、清晰、可调用。
