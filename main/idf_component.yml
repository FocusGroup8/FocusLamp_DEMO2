## IDF Component Manager Manifest File
## Reference: C:/Software/Espressif/.espressif/v5.5.4/esp-idf/examples/wifi/getting_started/station/main/idf_component.yml
dependencies:
  espressif/esp_wifi_remote:
    version: ">=0.10,<2.0"
    rules:
      - if: "target in [esp32p4, esp32h2]"
  espressif/esp_hosted:
    version: "~2"
    rules:
      - if: "target in [esp32p4, esp32h2]"
