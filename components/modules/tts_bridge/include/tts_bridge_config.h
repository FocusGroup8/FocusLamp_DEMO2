/*
 * tts_bridge_config.h - TTS Bridge target IP configuration
 */
#pragma once

#include "sdkconfig.h"

/* Target TTS board IP address (with port, e.g. "10.10.40.92:80") */
#define TTS_BRIDGE_TARGET_IP  CONFIG_TTS_BRIDGE_TARGET_IP
